procedure SPA {
	simple = 100;
	query = 25;
	sp = 1;
	pkb = 2;
	qps = 3;
	call SP;
	call PKB;
	while (query > 0) {
		print query;
		call QPS;
		read qps;
		if (qps > 25) {
			error = sp * pkb * qps;
			print error;
		}
	}
	if (sp + pkb + qps > 9 - 3) {
		moreThan6People = 1;
		while (1 > 0) {
			while (2 > 1) {
				print error;
			}
		}
		if (1 > 0) {
			read a;
		}
	}
	call call;
}
procedure SP {
	read simple;
	while (simple > 0) {
		sp = (10 * simple + sp) % simple;
		simple = simple / 2;
	}
	if (sp > 6) {
		read error;
		sp = error;
		pkb = error;
		qps = error;
		if (pkb > qps) {
			while (pkb > qps) {
				error = sp + qps % pkb - error;
				print error;
			}
		}
	}
	call meMaybe;
}
procedure PKB {
	storageA = 1;
	storageB = storageA + 2;
	print storageA;
	storageC = storageA * storageB;
	print storageB;
	print storageC;
	print storageD;
	storageD = storageA / storageB % storage C;
	call police;
}
procedure QPS {
	read pkb;
	read query;
	sp = query;
	call PKB;
	query = query - 1;
	call mrBean;
}